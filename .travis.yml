language: swift
os: osx
osx_image: xcode10.1
xcode_project: Flokk.xcworkspace
podfile: Podfile

before_install:
  - gem install cocoapods

install:
  - bundle install
  - pod repo update
  - pod install

script:
  - fastlane beta
  - fastlane scan FlokkTests
